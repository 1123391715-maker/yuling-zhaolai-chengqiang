# AI Engineering Constraints

适用项目：`御灵召来_城墙版`

这是项目级 AI 工程约束文件。  
所有 AI 辅助的 UI、美术、战斗表现、技能预览、资源接入、局部修改任务，都应默认遵守本文件。

配套细则见：

- `docs/AI美术UI接入工作流规范.md`

## 1. 最高原则

AI 不能把“好看的预览图”直接当作“可维护的工程资源”。

正式接入时，必须优先保证：

1. 可维护
2. 可复用
3. 可局部修改
4. 与现有组件一致
5. 与实机表现一致
6. 不浪费额度重复生成整图

视觉效果重要，但不能以牺牲工程结构为代价。

## 2. 需求类型判定

执行任何需求前，AI 必须先判断它属于哪一类：

| 类型 | AI 应做什么 | AI 不应做什么 |
|---|---|---|
| 方案讨论 | 只回答思路、风险、建议 | 不改文件 |
| 视觉探索 | 生成/讨论视觉图 | 不接入工程 |
| 工程拆解 | 输出资源清单、坐标、层级、组件复用方案 | 不直接写代码 |
| 资源生产 | 生成独立透明资源、九宫格资源、序列帧 | 不生成整张界面截图资源 |
| 工程接入 | 按资源清单接入代码 | 不重新设计 UI |
| 局部修改 | 只改目标组件/资源/逻辑 | 不从 0 重做整个界面 |
| 验收修正 | 基于实机截图修偏差 | 不凭感觉大改 |

如果用户说“先回答”“先说看法”“设计稿就行”，AI 不得擅自改工程。

## 3. 视觉图约束

AI 生成的整张 UI 图默认只是“视觉确认图”。

视觉图可以用于确认：

- 风格
- 构图
- 色调
- 层级关系
- 氛围
- 大致比例

视觉图不能直接用于：

- 截取矩形区域覆盖游戏画面
- 替换已有 UI 组件
- 烘焙动态文字
- 代替正式切图资源
- 代替工程组件

视觉图确认后，必须进入工程拆解阶段。

## 4. 资源接入约束

新增美术资源默认必须满足：

- 独立文件
- 真实 Alpha 通道
- PNG 或 WebP
- 不包含背景矩形底
- 不包含标注箭头、标注文字、选框、工具栏
- 不包含其他无关 UI 组件
- 能单独放回游戏画面中拼装

禁止：

- 截整张视觉图的一块贴进游戏
- 用带背景的法阵图覆盖场景
- 用截图替代按钮、血条、卡牌框
- 让动态数字/文字成为图片的一部分

允许：

- 场景背景作为完整不透明背景图
- 角色、法阵、光效、装饰、按钮框作为透明资源
- 面板/按钮/卡牌框使用九宫格
- 简单特效用代码绘制

## 5. 组件复用约束

新增 UI 前，必须先查现有组件。

优先复用：

- 现有按钮
- 现有血条
- 现有标题框
- 现有卡牌框
- 现有 HUD 控件
- 现有图标容器
- 现有字体、描边、阴影规则

只有当现有组件确实无法满足时，才允许新增组件。

新增组件时必须说明：

1. 为什么不能复用现有组件
2. 新组件用途
3. 后续是否复用
4. 有哪些状态
5. 是否需要九宫格

## 6. 局部修改约束

局部修改默认不得重新生成整张界面。

局部修改包括：

- 改坐标
- 改尺寸
- 改透明度
- 改文字
- 改按钮状态
- 改角色站位
- 改技能范围
- 改特效大小
- 替换单个图标
- 替换单个透明资源

局部修改必须遵循：

```text
定位目标
→ 判断影响范围
→ 只修改目标文件/目标函数/目标资源
→ 保持其他内容不变
→ 实机截图或语法检查
→ 如果偏差仍存在，继续局部修正
```

AI 不得因为一个小改动重写整个系统。

## 7. 代码接入约束

代码接入时必须：

- 尽量复用已有渲染函数
- 尽量复用已有配置结构
- 动态 UI 用程序绘制
- 坐标、尺寸、范围尽量配置化
- 预览页调用正式逻辑
- 不维护两套战斗/技能表现逻辑
- 不为了预览单独复制一份技能系统

禁止：

- 在预览页重新实现一套战斗表现
- 正式战斗和预览页分别改两份逻辑
- 把一次性视觉效果写死在多个位置
- 不经验证就宣称接入完成

## 8. 预览页约束

技能预览、战斗预览、UI 预览默认应该调用正式工程逻辑。

预览页可以拥有：

- 选择角色
- 选择强化
- 调整星级
- 调整速度
- 重启演示
- 显示调试信息

预览页不应该拥有：

- 独立技能伤害逻辑
- 独立特效逻辑
- 独立强化逻辑
- 与正式战斗不一致的范围/速度/命中规则

如果预览页与正式战斗表现不同，默认以正式战斗逻辑为准，并修正预览页调用方式。

## 9. 美术资源验收约束

新增资源必须通过以下检查：

### 9.1 Alpha 检查

- 背景是否透明
- 角落是否有残留底色
- 边缘是否有黑边/白边/灰边
- 是否能放在棋盘格背景上检查

### 9.2 场景检查

- 是否破坏原场景连续性
- 是否有矩形覆盖痕迹
- 是否有拼接缝
- 是否有明显色差

### 9.3 组件检查

- 是否与现有 UI 风格一致
- 是否能复用
- 是否支持状态变化
- 是否能适配不同屏幕比例

## 10. 工程完成定义

一个需求只有满足以下条件，才能称为“完成”：

- 代码已接入
- 资源已按组件拆分
- 新增图片资源已确认透明/九宫格/不透明类型
- 动态 UI 没有被图片烘焙
- 预览页与正式逻辑一致
- 已运行必要语法检查
- 已做实机截图或可视验证
- 已说明仍存在的偏差或限制

如果只做了视觉图，状态是“视觉确认”，不是完成。

如果只把截图裁进游戏，状态是“不合格接入”，不是完成。

如果没有截图/运行验证，状态最多是“初步接入”。

## 11. 默认执行顺序

当用户要求“接入游戏”“开始做”“按这个改”时，AI 默认按以下顺序执行：

```text
1. 识别需求类型
2. 查现有组件/资源/配置
3. 判断是否局部修改
4. 若涉及新增美术，先拆资源清单
5. 生成或准备透明资源
6. 接入工程
7. 更新缓存版本或资源索引
8. 运行语法/引用检查
9. 必要时打开预览或截图校验
10. 汇报修改内容、验证结果、后续风险
```

## 12. AI 回复约束

AI 汇报时必须区分：

- 已完成
- 已验证
- 未验证
- 需要用户确认
- 后续建议

不得把“计划做”“应该可以”“看起来能行”说成“已经完成”。

如果某项工作没有验证，必须明确说明。

## 13. 当前项目固定规则

针对本项目，以下规则长期有效：

1. 战斗法阵必须使用透明资源，不得矩形截图覆盖。
2. 角色资源必须使用已抠好的透明立绘。
3. 战斗下半部分只接需要的组件，血条下方暂未接入的 UI 可以预留空白。
4. 布阵界面和战斗界面的角色顺序必须一致。
5. 角色技能预览页必须调用正式战斗表现。
6. 强化卡必须明确生效角色、生效技能、普攻/大招/被动类型。
7. 新增普攻/强化时，优先实现正式战斗逻辑，再让预览页读取。
8. 小改动不得重做整张界面。
9. 新增 UI 组件优先复用现有风格。
10. 所有“视觉图转工程”必须先拆资源、再接入、再截图验收。

## 15. 效果图到工程组件的闭环约束

首充弹窗复盘确认：整张效果图是视觉构图真值，资源图集是工程素材来源；两者不能互相替代。

以后所有“效果图转 UI”任务必须建立可核对的组件映射表，逐项列出：

1. 效果图中每个可见组件：面板、标题装饰、侧挂饰、角色承托、立绘、特效、奖励框、图标、按钮、文字和状态。
2. 每个组件的实现方式：复用现有资源、新增透明切图、九宫格、代码绘制或明确不接入。
3. 每个新增切图的源图、文件名、透明检查和代码调用位置。
4. 每个已注册资源的实际调用位置；只注册不绘制，仍视为未完成。

硬性规则：

- 效果图中存在的装饰或边框，不能未经说明被代码画框替代。
- 已有批准切图时，不能用 `Canvas` 线框、圆角矩形或临时占位框替代。
- 不能因为手里有一张切图，就把按钮框、英雄框或其他不相同组件拉伸冒充奖励框。
- 角色外框只有在效果图明确存在时才允许接入；透明立绘本身不自动添加矩形承托框。
- 奖励卡/道具框必须使用对应的奖励框资源；动态图标、名称、数量仍由程序绘制。
- 整张效果图只能作为视觉参考和组件定位依据；运行时正式资源必须是独立导出的透明 PNG/WebP，不能把效果图截图裁一块、清空中间内容后继续当正式切图。
- 资源图集中没有效果图所需组件时，必须记录为“资源缺口/初步接入”，不能宣称正式还原完成。
- 接入后必须以游戏内截图逐项对照效果图；未完成视觉对照时，最多报告“代码与资源初步接入”。

这次首充弹窗的具体教训：立绘外框和奖励道具框都被错误地用代码近似，且部分已切装饰没有调用；后续必须先完成组件映射，再进入代码拼装。

## 14. 推荐用户指令

如果用户希望 AI 严格执行本文件，可以直接说：

```text
按 AI_ENGINEERING_CONSTRAINTS.md 执行。
```

如果是 UI / 美术接入，可以说：

```text
按 AI工程约束和 AI美术UI接入工作流规范执行。
先拆资源清单，不要直接裁截图覆盖。
```

如果是局部修改，可以说：

```text
这是局部修改，不要重新生成整张界面。
只改目标组件/资源/代码，并做验证。
```

## 16. UI 坐标契约与视觉差异门禁

从 2026-08-20 起，游戏 UI 统一使用 `config/ui-layout.js` 声明的逻辑画布：`750×1334`，坐标单位为逻辑画布像素。浏览器窗口、微信设备分辨率和 DPR 只能改变整张 Canvas 的缩放，不能改变组件的 `x/y/w/h`。

硬性规则：

1. 新 UI 的布局必须写入 `YL.UI_LAYOUT`，绘制代码和点击热区都从同一份矩形读取；禁止在渲染函数里再写一套凭感觉的坐标。
2. 视觉图进入工程前必须记录设计画布尺寸、目标安全区和组件矩形；非 `750×1334` 的原图必须先经过明确的基准化步骤，不能让视觉差异工具隐式按另一张图的尺寸比较。
3. 资源的原始像素尺寸、显示矩形和裁切策略必须分开记录。禁止把不同宽高比的资源直接拉伸到同一个矩形来“对齐”。
4. 动态文字必须登记固定显示矩形；超出时按“缩小字号 → 在矩形内换行 → 必要时省略”处理，禁止无边界 `fillText` 覆盖图标、按钮或边框。
5. 每个状态都要有游戏内截图，并通过叠图或差异检查；只有代码检查通过而没有视觉证据时，只能写“初步接入”。
6. `tools/validate-ui-layout.mjs` 是提交前的坐标契约检查；`tools/ui-visual-diff.py` 默认拒绝未基准化的视觉图，确需缩放必须显式传入参数并在验收记录中说明。
7. 旧界面逐步迁移时，先把现有坐标集中登记到契约，再逐组件测量修正；不得因为建立契约就声称旧页面已经与效果图完全一致。

固定执行顺序：

```text
测量确认视觉图 → 写入布局契约 → 拼静态层 → 截游戏内图
→ 叠图/差异检查 → 修正坐标与比例 → 再接状态和交互 → 复测
```
