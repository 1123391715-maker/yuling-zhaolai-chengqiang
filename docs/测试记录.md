# 测试记录

## 2026-07-17 · 符箓总览 UI v1

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| JavaScript 语法 | 通过 | `node --check src/game-v2.js` 无输出、退出码 0 |
| 新增资源加载路径 | 通过 | 6 个符箓 PNG 均存在；alpha 资源角落透明 |
| 符箓入口 | 通过 | 实机战斗界面可见于右侧倍速下方，点击可打开总览 |
| 打开 / 关闭暂停态 | 通过 | 打开总览后战斗冻结；点击右上角 X 后恢复战斗 |
| 空状态 | 通过 | `artifacts/talisman-ui-runtime-empty-v1.png` |
| 徽章计数 | 通过 | `artifacts/talisman-ui-runtime-badge-v1.png`；获得强化后角色脚侧出现数量徽章 |
| 定位叠图 | 通过 | `artifacts/talisman-ui-overlay-v1.png`；主面板、标题、关闭点、三条列表区域与标注相符 |
| 非空列表翻页 | 代码覆盖，待下一次含 4 条生效强化的实机回归 | 逻辑由 `activeTalismanUpgrades`、`talismanScroll` 与底部翻页热区实现 |

### 校准结论

- 初版标题中心高出生成底板标题牌约 34 逻辑像素；已从 `(375,190)` 校正为 `(375,224)`。
- 当前验收偏差未超过 3px 的项目：面板外框、关闭按钮和三条列表预留区。
